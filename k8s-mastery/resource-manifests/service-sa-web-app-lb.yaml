apiVersion: v1
kind: Service
metadata:
  name: sa-web-app-lb          
spec:
  type: ClusterIP  
  selector:
    app: sa-web-app        
  ports:
    - port: 8080          
      targetPort: 8080
      protocol: TCP


# apiVersion: v1
# kind: Service
# metadata:
#   name: sa-web-app-lb
# spec:
#   type: LoadBalancer
#   ports:
#   - port: 80
#     protocol: TCP
#     targetPort: 8080
#   selector:
#     app: sa-web-app

